This contains the react code used to render div id `root` on Task List Page. 

if there is changes on the sample.js, it must be compiled for use by Asp.NET 

To run the compiler, run `npm run webpack` and it will compile sample.js then outputs the result in dist/bundle.js. 

